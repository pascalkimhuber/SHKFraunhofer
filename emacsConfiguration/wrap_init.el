;;; Enable visual-line-mode by default (whole words are wrapped at the end of line).
(setq-default visual-line-mode t)

;;; In order to toggle visual-line-mode in a buffer, use:
;;; M-x visual-line-mode.

;;; In order to toggle truncated lines in a buffer, use:
;;; M-x truncate-lines. 
